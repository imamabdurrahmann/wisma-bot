# Core dependencies for Wisma Bot
# Python 3.10+ required

# Optional: asyncio-ring (for enhanced async support)
# Uncomment if needed for production use
# asyncio-ring>=0.5.0

# For WhatsApp integration options:
# Option 1: wa-js (Node.js based, recommended for Termux)
#   Install Node.js in Termux, then: npm install wa-js
#
# Option 2: WhatsApp Web API (third-party services)
#   Check available Python packages or use webhook server
#
# Option 3: WhatsApp Business API
#   Requires Facebook Business account

# For running tests without WhatsApp:
# No additional dependencies needed - built-in asyncio used
# Just run: python main.py --test